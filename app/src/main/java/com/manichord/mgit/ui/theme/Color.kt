package com.manichord.mgit.ui.theme

import androidx.compose.ui.graphics.Color

// Brand seed color -- same green used in the app icon (a sprout/branch-fork mark).
// The full tonal palette (light + dark, all M3 color roles) is generated from this
// single seed via MaterialKolor rather than hand-picked, so it stays correctly
// contrasted and in sync if this color ever changes.
val BrandSeedColor = Color(0xFF1B5E3A)
